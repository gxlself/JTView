// import { mapGetters } from 'vuex'

export default {
  name: 'Layout',
  computed: {
    // ...mapGetters(['menus'])
  },
  methods: {
    logout (key) {
      // this.$store.dispatch('user/logout')
      // this.$router.push('/login')
    }
  }
}
